#include "main.h"

/**
 * _abs - Computes abs value of an integer.
 *
 *@num: Integer.
 *
 * Return: absolute value of input.
 */
int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
